name: 'Manage release information in extension files'
description: 'Automatically change release version'
inputs:
  repository:
    description: 'Repository name'
    required: true
    default: ''
  action:
    description: 'Choose what the cation should do, initial_changelog_update, check_changlog_updated'
    required: true
    default: 'initial_changelog_update'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.repository }}
